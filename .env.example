# API origin only — no trailing slash. Client prefixes paths with /api/v1/users.
# Match CORS_ORIGIN on the server for cookie auth; otherwise use Bearer token (default in this app).
# OpenAPI: {VITE_API_BASE_URL}/api-docs
VITE_API_BASE_URL=https://bizxflow-production.up.railway.app
